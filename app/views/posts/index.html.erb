
<% for post in @posts %>

	<%= render :partial => "post", :object => post  %>
	<%=  link_to number_of_comments(post), post_path(post, :anchor => "comments") %>

<% end %>

<%= link_to 'New post', new_post_path %>
